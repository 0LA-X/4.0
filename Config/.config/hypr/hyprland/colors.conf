general {
    col.active_border = rgba(94909177)
    col.active_border = rgb(800080) rgb(39FF14) rgb(FF6EC7) rgb(800080) rgb(39FF14) rgb(FF6EC7)
    
  #  col.inactive_border = rgba(3C383Eee) col.inactive_border = rgba(49464855)
}

misc {
    background_color = rgba(141314FF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Google Sans Flex Medium, Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(141314FF)
        col.text = rgba(e7e1e3FF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(e7e1e3), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(e7e1e3), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(e7e1e3), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrulev2 = bordercolor rgba(d1c2d2AA) rgba(d1c2d277),pinned:1
